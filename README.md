# Business Service Platform — Railway

Проект подготовлен для запуска на Railway без собственного домена.

Railway автоматически запускает:
`npm start`

Сервер слушает `0.0.0.0` и порт из переменной `PORT`.

После деплоя:
**Settings → Networking → Generate Domain**
